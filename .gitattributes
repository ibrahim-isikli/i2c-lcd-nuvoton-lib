# Auto detect text files and perform LF normalization
* text=auto

# Release klasörü içindeki tüm dosyaları analiz dışı bırak
i2c-lcd-nuvoton-lib/lcd_for_nuvoton/Release/** linguist-detectable=false

# GCC klasörü de analiz dışı kalsın
i2c-lcd-nuvoton-lib/lcd_for_nuvoton/GCC/** linguist-detectable=false

# Tüm .o ve .d uzantılı dosyaları analiz dışı bırak
*.o linguist-detectable=false
*.d linguist-detectable=false

# Makefile analiz dışı
**/Makefile linguist-detectable=false
